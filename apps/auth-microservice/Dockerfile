# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build auth-microservice`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t auth-microservice`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system auth-microservice && \
          adduser --system -G auth-microservice auth-microservice

COPY dist/apps/auth-microservice auth-microservice
RUN chown -R auth-microservice:auth-microservice .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix auth-microservice --omit=dev -f install

CMD [ "node", "auth-microservice" ]
